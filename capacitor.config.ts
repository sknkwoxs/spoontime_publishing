import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.github.sknkwoxs.spoontime',
  appName: 'spoontime',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
